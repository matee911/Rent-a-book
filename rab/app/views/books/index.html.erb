
<%= link_to 'Dodaj', resource(:books, :new) %>
<hr />
<h2>Książki</h2>

<ul>
  <% @books.each do |book| %>
  <li>
    <% if book.has_cover? %>
      <%= image_tag book.cover_thumbnail %>
    <% end %>
    <%= link_to "#{book.title}", resource(book) %>
  </li>
  <% end %>
</ul>
