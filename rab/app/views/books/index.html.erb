
<h2>Książki</h2>

<%= link_to 'New', resource(:books, :new) %>

<hr />

<ul>
  <% @books.each do |book| %>
  <li><%= link_to "#{book.title}", url(:controller => 'books', :action => 'show', :id => book.id) %></li> <!-- <%= link_to 'Edit', resource(book, :edit) %> <%= book.title %> -->
  <% end %>
</ul>
